{
  "name": "kekstagram",
  "version": "22.0.0",
  "private": true,
  "description": "Личный проект «{{projectTitle}}» от HTML Academy",
  "repository": {
    "type": "git",
    "url": "{{sshUrl}}"
  },
  "bugs": {
    "url": "{{bugsUrl}}"
  },
  "devDependencies": {
    "eslint": "7.18.0",
    "eslint-config-htmlacademy": "2.0.0",
    "webpack": "5.98.0",
    "webpack-cli": "6.0.1"
  },
  "license": "MIT",
  "scripts": {
    "build": "webpack --mode production",
    "start": "browser-sync start --server \"build/\" --files \"source/**/*.js\"",
    "test": "eslint source/js/"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "dependencies": {
    "browser-sync": "^1.3.7"
  }
}
